<Window x:Class="FrameCreation_WPF.View.Tool_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Your Task!" 
        Height="280" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Number of Frames -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="40,15,20,10" VerticalAlignment="Center">
            <TextBlock Text="Number of Frames" VerticalAlignment="Center" FontSize="16" Width="180" FontWeight="Medium"/>
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3">
                <TextBox x:Name="txtNumberOfFrames" Width="100" Height="30" Text="4" FontSize="16" 
                         BorderThickness="0" Padding="5"
                         HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Spacing -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="40,10,20,10" VerticalAlignment="Center">
            <TextBlock Text="Spacing" VerticalAlignment="Center" FontSize="16" Width="180" FontWeight="Medium"/>
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3">
                <TextBox x:Name="txtSpacing" Width="100" Height="30" Text="5" FontSize="16"
                         BorderThickness="0" Padding="5"
                         HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            </Border>
            <TextBlock Text="m" VerticalAlignment="Center" FontSize="16" Margin="8,0,0,0" FontWeight="Medium"/>
        </StackPanel>

        <!-- Instructions -->
        <TextBlock Grid.Row="2" Text="Press To Create Frame In SAP2000" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="13" Foreground="#555555"/>

        <!-- Create Button -->
        <Button Grid.Row="3" x:Name="btnCreate" Content="Create!" 
                Width="140" Height="45" FontSize="16" FontWeight="Bold"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Margin="0,10,0,0"
                Click="Button_Click" Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#2C3E50"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4" Padding="15,8">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#34495E"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#1A252F"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>